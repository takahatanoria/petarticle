- if profile.errors.any?
  - unless profile.valid?(:validates_step2)
    - if errors = profile.errors.full_messages_for(column.to_sym)
      %ul.has-error-text
        - errors.each do |error|
          %li=error